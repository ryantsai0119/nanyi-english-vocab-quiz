# 聽寫測驗（Dictation）功能計畫

目標站點：https://ryantsai0119.github.io/nanyi-english-vocab-quiz/  
公開倉庫：https://github.com/ryantsai0119/nanyi-english-vocab-quiz  
本 ERP 目錄為副本：`web/english-vocab-quiz/`（部署請以公開倉庫為準）

---

## 1. 目標

在現有「單字測驗」旁新增 **聽寫／聽力打字練習**：

1. 依 **課次－區段** 選題（如 `1-1`、`1-2`、`1-3`、`2-1`…）
2. 將該區段拆成多個 **短句／短段**
3. **循環播放** 目前這一小段英文（TTS）
4. 使用者打字作答（**每個字母一個空格位置；標點不用輸入**）
5. 按「對答案」得到該段 **完成百分比分數**
6. 做完該區段所有小段後，算 **平均分**；寫入本機紀錄，**僅在更高分時更新**

素材來源：`listening-transcripts/`（超群新幹線第三冊聽力課文轉寫）。

---

## 2. 課次編碼（1-1 / 1-2 / 1-3）

| 代碼 | 意義 | 內容來源 |
|---|---|---|
| `L-1` | Lesson L，第 1 區 | 一、辨識句意（每句＝1 小段） |
| `L-2` | Lesson L，第 2 區 | 二、基本問答（每題＝1 小段） |
| `L-3` | Lesson L，第 3 區 | 三、言談理解（見下方切段規則） |

範例：`1-1`＝第 1 課辨識句意；`2-3`＝第 2 課言談理解。

**Review（複習）**（可選第二期）：

| 代碼 | 意義 |
|---|---|
| `R1-1` / `R1-2` / `R1-3` | Review 1 三區 |
| `R2-*` / `R3-*` | Review 2／3 |

第一期先做 **Lesson 1–6 × 三區**（共 18 個可選區段）。

### 2.1 言談理解（`L-3`）切段規則

對話較長，建議兩種模式（UI 可切換，預設 A）：

- **A. 逐句模式（建議預設）**：Dialogue 內每一句英文各自成一小段；`Question:` 列可選「併入最後一句」或「獨立一小段」。
- **B. 整段對話模式**：一個 Dialogue（含 Question）＝一小段，TTS 連續唸完再循環。

第一期實作 **A**，較符合「不斷循環一小段」。

---

## 3. 使用流程（畫面）

```
首頁
 └─ [聽寫練習] 新入口（與「開始測驗／各課單字表」並列）
      ├─ 選課網格：1-1 1-2 1-3 / 2-1 2-2 2-3 / … / 6-1 6-2 6-3
      │     （格子上顯示該區段歷史最高分，若有）
      └─ 進入區段練習
           ├─ 進度：第 k / n 小段
           ├─ 播放列：▶ 循環播放｜⏸ 暫停｜下一句｜重播
           ├─ 作答區：字母空格（見 §5）
           ├─ [對答案] → 顯示本段完成％、正確句、下一段
           └─ 區段結束 → 平均分、是否破紀錄、回選課
```

風格沿用現有 brick／paper 視覺與虛擬鍵盤（可複用現有 keyboard 邏輯）。

---

## 4. 語音播放（TTS）

### 4.1 首選：瀏覽器 Web Speech API（已用於現有「聽力測驗」）

```js
const u = new SpeechSynthesisUtterance(sentence);
u.lang = 'en-US'; // 備援 en-GB
u.rate = 0.9;    // 可調慢速 0.75–1.0
speechSynthesis.speak(u);
```

- **循環**：`utterance.onend` → 若仍在循環模式且未暫停 → 再 `speak` 同一句。
- **暫停／停止**：`speechSynthesis.cancel()`。
- 進入小段時 **自動開始循環**；對答案或切換小段時停止。

### 4.2 備援（第二期可選）

| 方案 | 說明 |
|---|---|
| 挑選特定 Google / Microsoft 英文 voice | `getVoices()` 過濾 `en-*`，記住使用者選擇 |
| 原版 MP3 片段 | 需額外切片時間軸，成本高；第一期不做 |

不依賴需 API Key 的雲端 TTS，以利 GitHub Pages 純靜態部署。

---

## 5. 作答規則（字母空格／標點）

### 5.1 顯示

正確句正規化後，**每個英文字母佔一個輸入格**，字母格之間以視覺空格分開；**單字之間加大間距**。

範例句子：`I watched a ghost movie last night.`

顯示（示意）：

```
_   _ _ _ _ _ _ _   _   _ _ _ _ _   _ _ _ _ _   _ _ _ _   _ _ _ _ _ _
I   w a t c h e d   a   g h o s t   m o v i e   l a s t   n i g h t
```

- 標點（`. , ? ! ' -` 等）**不佔必填格**；可淡色預先顯示在字旁，或完全隱藏。
- 縮寫如 `didn't`：建議把 `'` 當標點略過，字母格為 `d i d n t`（或保留 `'` 為固定顯示、不必輸入）——實作時二選一寫進資料的 `normalize` 函式並在 UI 說明。

### 5.2 輸入

- 只接受 A–Z（自動轉小寫比對或統一小寫儲存）。
- 空白鍵＝跳到下一個「單字」起點（可選便利功能）。
- Backspace＝刪前一格字母。
- 沿用／延伸現有螢幕鍵盤。

### 5.3 對答案計分（完成百分比）

對 **字母格** 逐格比對（忽略大小寫）：

```
完成％ = round( 答對字母數 / 應填字母總數 × 100 )
```

- 未填空白＝該格不算對。
- 標點不參與計分。
- 單段分數存起來；**區段平均**＝該區所有小段完成％的算術平均（四捨五入到整數）。

「對答案」後：

1. 顯示本題完成％與正確句子（可再播一次 TTS）
2. 鎖定本段輸入，按「下一題」進下一段
3. 最後一段結束 → 顯示區段平均、是否更新最高分

---

## 6. 資料格式

由 `listening-transcripts/lesson-XX.txt` 建置靜態 JS／JSON（類似 `vocab-data.js`）。

建議檔案：`dictation-data.js`

```js
const dictationBank = {
  "1-1": {
    "id": "1-1",
    "lesson": 1,
    "section": 1,
    "title": "Lesson 1｜辨識句意",
    "segments": [
      {
        "id": "1-1-01",
        "text": "My uncle was an English teacher ten years ago, but now he is a farmer.",
        "speak": "My uncle was an English teacher ten years ago, but now he is a farmer."
      }
      // ...
    ]
  },
  "1-2": { /* 基本問答 */ },
  "1-3": { /* 言談理解逐句 */ }
  // ... "6-3"
};
```

建置腳本（公開倉庫）：`tools/build_dictation_data.py`

- 解析 `## 一、` / `## 二、` / `### Dialogue` 
- `1-3` 依逐句切開；過濾空行與 `**Question:**` 前綴（Question 句子本身可納入 speak）
- 輸出字母格所需的 `lettersOnly` 可執行期計算，不必預存

---

## 7. 分數紀錄（本機）

使用 `localStorage`（與現有「已背」勾選同一模式）：

```js
// key: nanyi-dictation-scores-v1
{
  "1-1": { "best": 92, "updatedAt": "2026-07-19T..." },
  "1-2": { "best": 80, "updatedAt": "..." }
}
```

規則：

- 區段結束算出 `avg`
- 若無紀錄，或 `avg > best` → 更新
- 選課網格顯示 `best`（尚無則顯示「—」）

（第二期可選：匯出／匯入 JSON、依日期保留歷次紀錄。）

---

## 8. 實作分期

### 第一期（可上線 MVP）

1. `tools/build_dictation_data.py` + `dictation-data.js`（Lesson 1–6 × 三區）
2. 首頁新增「聽寫練習」入口與選課網格 `1-1`…`6-3`
3. 區段練習頁：循環 TTS、字母空格輸入、對答案、完成％
4. 區段平均分 + `localStorage` 最高分更新
5. 複用現有 `speak()`／虛擬鍵盤樣式
6. 同步更新公開倉庫 GitHub Pages

### 第二期（體驗加強）

- Review `R1-1`… 納入選課
- 語速／男聲女聲／循環間隔（句與句之間静音 0.4s）
- 言談理解「整段模式」
- 單段可「再練一次」不影響已鎖定的最高分計算方式說明
- 校正 Whisper 轉寫錯字（對課本人工修 `listening-transcripts` 後重建 data）

### 第三期（可選）

- 與單字 bank 連動：聽寫句中高亮本課單字
- 錯字回放：只循環念錯的單字
- 簡單統計圖（各區段最高分長條）

---

## 9. 檔案異動預估（公開倉庫為主）

| 檔案 | 動作 |
|---|---|
| `index.html`（或拆 `dictation.html`） | 新增聽寫 UI／流程 |
| `dictation-data.js` | 新建，由腳本產生 |
| `tools/build_dictation_data.py` | 新建 |
| `listening-transcripts/*.txt` | 已有；人工校正後重建 |
| `README.md` | 說明聽寫用法與編碼 |

若拆成 `dictation.html`，首頁用連結跳轉，可減少與單字測驗狀態互相干擾。

---

## 10. 驗收標準

- [ ] 可選 `1-1`…`6-3` 進入對應區段
- [ ] 進入小段後英文句 **自動循環播放**，可暫停／續播
- [ ] 字母格數量＝句子英文字母數；標點不必輸入
- [ ] 「對答案」顯示 0–100% 完成度
- [ ] 區段全部完成後顯示平均分
- [ ] 平均分高於歷史最高才寫入；選課格看得到最高分
- [ ] 手機可完成主要流程（鍵盤＋播放鈕）
- [ ] 純靜態、無後端、無 API Key

---

## 11. 風險與注意

1. **轉寫品質**：Whisper 課文可能有錯字 → 上線前建議至少校對 Lesson 1–2；錯字會直接變成「標準答案」。
2. **iOS 語音**：部分 Safari 需使用者手勢後才能 `speak`；進段時用「開始練習」按鈕觸發第一次播放。
3. **對話句切段**：自動依句號切開可能把 `Mr.` 誤切 → 建置腳本需簡單保護（`Mr.` / `Ms.` / `Dr.` / 小數）。
4. **部署位置**：功能應合進 **nanyi-english-vocab-quiz** 倉庫；ERP 內副本可事後同步。

---

## 12. 建議下一步

1. 確認 `L-3` 採「逐句」還是「整段對話」為預設（計畫預設逐句）。
2. 確認縮寫 `didn't` 的字母格規則（建議：`'` 顯示但不計分）。
3. 核准後在公開倉庫實作第一期 MVP，並用 Lesson 1 三區做手動驗收。
