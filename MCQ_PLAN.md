# 課程選擇題測驗（MCQ）功能計畫

目標站點：https://ryantsai0119.github.io/nanyi-english-vocab-quiz/  
公開倉庫：https://github.com/ryantsai0119/nanyi-english-vocab-quiz  
本目錄副本：`web/english-vocab-quiz/`

教材：南一版｜超群新幹線第三冊（八上／國中二上）Lesson 1–6｜115 學年度

---

## 1. 目標

新增 **「課程選擇題」** 測驗模式，讓學生能：

1. 依 **課次題庫**（Lesson 1–6，可多選）出題  
2. 從該範圍 **隨機抽題**（可設 10／20／30／全部）  
3. 以 **四選一**（為主）作答，即時對錯回饋可選  
4. 結束後 **計分結算**（分數、答對題數、錯題回顧）  

與既有功能關係：

| 功能 | 用途 |
|---|---|
| 開始測驗（中翻英／英翻中／聽力） | 單字產出型／聽力 |
| 聽寫練習 | 聽力＋拼寫 |
| 課程學習（文法小測驗） | 每課僅約 3 題、嵌在文法文末 |
| **課程選擇題（本計畫）** | **大量選擇題題庫＋隨機抽＋計分** |

---

## 2. 能不能「找到」南一這一冊的官方選擇題？

### 2.1 結論（重要）

| 來源 | 找不找得到 | 能否直接放進本站 |
|---|---|---|
| 南一官方 **Nani Paper／備課／挑戰卷／命題光碟** | 有（教師／學校授權管道） | **否**（著作權；公開 GitHub Pages 不可整卷轉貼） |
| 市售「南一命題光碟／挑戰卷」轉檔站 | 網路上可見列表 | **否**（多屬未授權散佈，不可爬取／匯入） |
| 學校老師發的練習卷 PDF | 若家中有紙本／檔 | **僅可私用**；要上公開站需自行改寫或取得授權 |
| 本站既有素材（單字／文法／聽力稿／課文） | 已有 | **可** → 依課綱 **自製原創題**（建議主路徑） |

**實務建議：** 不要把「南一挑戰卷／光碟原題」原封不動貼進公開站。題庫以 **對齊本冊文法＋單字＋課文情境的原創選擇題** 為主；若你有紙本考卷，可當「題型參考」（句型、考點），由我們改寫成新題再進庫。

### 2.2 合法取得官方題（若堅持接近原卷）

1. 學校老師帳號 → [南一數位資源入口網](https://nanidigi.nani.com.tw/) → Nani Paper／Nani Box  
2. 學校採購之「命題光碟／備課素材庫」（僅教用）  
3. 若要進本站：請提供 **可公開使用的同意**，或只在 **本機／私有** 環境使用  

本計畫 **預設走「自製題庫」**，不依賴未授權原卷。

---

## 3. 既有可擴充素材（題量估算）

| 素材 | 現況 | 可轉成的選擇題類型 |
|---|---|---|
| `vocab-data.js` | L1–L6 約 **254** 字（40／37／38／42／47／50） | 中英對譯、克漏字（sentence 欄）、詞性／搭配 |
| `grammar-data.js` | 每課約 **3** 題二選一（共 ~18） | 擴成四選一；依 traps／examples 再產題 |
| `listening-transcripts/` | 各課 1-1／1-2／1-3＋Review | 句意選擇、對話回應、聽力文字版理解 |
| `reading-data.js` / `grammar-data.js` | 課文區段＋文法句型 | 文意選擇、文法改錯選正確句 |

**題量目標（建議）：**

| 階段 | 每課 | 全冊約 |
|---|---|---|
| MVP | ≥ 40 題 | ≥ 240 題 |
| 目標 | ≥ 80 題 | ≥ 480 題 |
| 衝量 | ≥ 120 題 | ≥ 720 題 |

「多一點」靠：**規則產題（單字／文法）＋人工精修高價值題（對話／文意）**。

---

## 4. 題型設計（課程選擇題）

每題統一結構（四選一為預設）：

```js
{
  id: "L1-G-001",          // 穩定 ID，利於錯題本／去重
  lesson: "Lesson 1",
  type: "grammar",         // vocab | grammar | cloze | dialogue | reading
  stem: "題幹（中或英）",
  options: ["A", "B", "C", "D"],
  answer: 0,               // 0–3
  explain: "簡短解析",
  tags: ["past-be", "was-were"]  // 可選
}
```

### 4.1 類型與佔比（建議）

| type | 說明 | 建議佔比 | 產題方式 |
|---|---|---|---|
| `grammar` | 選正確句／填適當動詞形式 | 35% | 依文法重點＋traps 模板 |
| `vocab` | 中→英或英→中（選項為單字） | 25% | 同課干擾選項自動抽 |
| `cloze` | 句子空格選字 | 20% | 用 `sentence` 欄位＋同課字 |
| `dialogue` | 依情境選最佳回應 | 15% | 聽力稿改寫（人工為主） |
| `reading` | 短文／短對話文意 | 5% | 課文區段出題（後期） |

---

## 5. 入口與畫面流程

### 5.1 首頁「開始測驗」模式區

在既有按鈕旁新增：

```
[ 中翻英 ] [ 英翻中 ] [ 聽寫練習 ] [ 聽力測驗 ] [ 課程選擇題 ]
```

沿用現有：

- 課次勾選（全選／全不選）  
- 題數：10／20／30／全部  

### 5.2 作答流程

```
選課＋題數 → 從合併題庫隨機抽 N 題（不重覆）
  → 顯示題幹＋A–D
  → 選答 → 下一題（可選：當場顯示對錯＋解析）
  → 結束頁：分數、答對／總題、錯題列表（題幹／正解／你的答案／解析）
  → 重新測驗／回首頁
```

### 5.3 計分

- 分數 = `round(正確數 / 總題數 * 100)`  
- 可選：localStorage 記錄「本課最高分」（與聽寫類似）  

---

## 6. 資料與程式結構

### 6.1 新檔

| 檔案 | 用途 |
|---|---|
| `mcq-data.js` | 靜態題庫（`mcqBank`） |
| `mcq.js` | 抽題、作答 UI、計分（或先寫進 `index.html` 再拆） |
| `tools/build_mcq_from_vocab.py` | 由單字表產 vocab／cloze 題草稿 |
| `tools/build_mcq_from_grammar.py` | 由文法 traps／examples 產 grammar 題草稿 |
| `MCQ_PLAN.md` | 本計畫 |

### 6.2 資料形狀

```js
const mcqBank = {
  "Lesson 1": [ /* question objects */ ],
  "Lesson 2": [ /* ... */ ],
  // ...
};
```

抽題邏輯（與現有 `startGame` 對齊）：

1. 收集勾選課次的所有題  
2. `shuffle` 後取前 N 題  
3. 若總題不足 N → 用全部並提示  

### 6.3 規則產題範例（單字）

- 正確答案：本課某字 `en`／`zh`  
- 干擾項：同課其他字 3 個（不足則跨鄰課）  
- cloze：把 `sentence` 的 `___` 當空格，選項為正確字＋3 干擾  

需 **過濾**：太短／同義易混／選項重複 → 產完人工抽查。

### 6.4 規則產題範例（文法）

從每課 `traps`：

- 題幹：「下列哪一句正確？」  
- options：`right` + 改寫／其他 traps 的 `wrong`（打亂）  
- explain：沿用 `note`  

從 `examples`：挖空關鍵詞（was／didn't／V-ed）做四選一。

---

## 7. 分期實作

### Phase 1 — MVP（可先上線）

1. UI：新增「課程選擇題」模式＋四選一畫面＋結算頁  
2. 題庫：每課 **≥40**（優先 grammar＋vocab＋cloze 規則產＋微調）  
3. 隨機抽題、計分、錯題列表  
4. 同步：ERP `web/english-vocab-quiz/` → 公開 repo → Pages  

### Phase 2 — 變多、變像考卷

1. 每課衝到 **≥80**  
2. 加入 dialogue 題（聽力稿改寫）  
3. 選項固定四個；支援「作答後顯示解析」開關  
4. 本課最高分 localStorage  

### Phase 3 — 精修

1. reading／短文題  
2. Review 綜合卷（跨課）  
3. 錯題本（只練錯過的 id）  
4. 若你提供紙本考卷照片：依考點 **改寫進庫**（不原文照貼）  

---

## 8. 風險與規範

| 風險 | 處置 |
|---|---|
| 侵權（官方卷／光碟） | 禁止原題進公開站；改寫或自製 |
| 規則產題品質不穩 | 產題腳本＋抽樣人工檢查；標記 `source: "auto"` |
| 與舊「中翻英」重複感 | 選擇題強調文法／情境／克漏字，不只對譯 |
| Pages 快取 | 資產加 `?v=` 版號（沿用聽寫作法） |

---

## 9. 驗收標準

- [ ] 首頁可進「課程選擇題」  
- [ ] 可多課勾選＋指定題數隨機出題  
- [ ] 四選一作答，結束有分數與錯題回顧  
- [ ] L1–L6 各有獨立題庫；MVP 全冊 ≥240 題  
- [ ] 本機／公開站行為一致  
- [ ] 題目註記為自製／改寫，無未授權原卷全文  

---

## 10. 已確認決策（2026-07-30）

| 項目 | 決定 |
|---|---|
| 公開站題庫 | **A 純自製**（對齊本冊單字／文法／課文，原創選擇題） |
| 官方卷／光碟 | **C 僅本機／家庭自用**，**不上** GitHub／Pages；可放本機忽略目錄（見下） |
| 用途 | **非商用**，孩子自主學習 |
| 作答回饋 | 學習向：**選答後先顯示對錯＋短解析**，再下一題；結束仍有總分與錯題表 |

### 本機官方卷放置（不上版控）

若之後有挑戰卷／習作 PDF，建議放在：

`web/english-vocab-quiz/private-exams/`（已列入 `.gitignore`）

僅本機開啟複習；公開站仍只用 `mcq-data.js` 自製題。

---

## 11. 實作狀態

- [x] 計畫與 A+C 決策  
- [x] Phase 1 MVP（題庫＋UI＋計分；全冊約 864 題原創）  
- [ ] Phase 2 對話／文意精修題  
- [ ] 同步公開 repo／Pages（部署時）  
